\* TLC configuration for Helix.tla — N=4, f=1, two candidate blocks.
CONSTANTS
    Validators = {v1, v2, v3, v4}
    Byz = {v4}
    Blocks = {b1, b2}

INIT Init
NEXT Next

INVARIANT Agreement
INVARIANT NoCorrectEquivocation
