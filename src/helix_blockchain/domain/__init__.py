"""Pure domain layer: cryptography, Merkle trees, blocks and the chain.

This package has no I/O dependencies (no database, network or FIWARE access),
which keeps it fully unit-testable in isolation.
"""
